node_modules/
build/
# ...but the Electron build resources (app icons) are source, not output —
# without them nobody who clones this repo can package the desktop app.
!apps/desktop/build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Next.js
.next/
out/
tsconfig.tsbuildinfo
next-env.d.ts

# Go backend
apps/backend/palius-backend
apps/backend/*.exe
*.db
*.db-shm
*.db-wal
*.deb

# Vercel
.vercel

# Local-only compose overrides (machine-specific DNS, ports, etc.)
docker-compose.override.yml

# Internal business doc — pricing and unit economics, not for the public repo
BILLING.md
